{
  "name": "ada-lens",
  "version": "1.0.0",
  "description": "A chrome extension that will find images not compliant with accessibility and add accessibility features.",
  "type": "module",
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "@types/node": "^24.0.13",
    "typescript": "^5.8.3",
    "vite": "^7.0.4"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch"
  },
  "dependencies": {
    "chrome-types": "^0.1.361"
  }
}
